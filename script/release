#!/usr/bin/env bash

set -euo pipefail

if [ "$#" -lt 1 ] || [ "$#" -gt 2 ]; then
  echo "Usage: $0 <version> [dist-tag]" >&2
  exit 1
fi

VERSION=$1
DIST_TAG=${2:-latest}

gh workflow run publish.yml --ref "v${VERSION}" -f version="$VERSION" -f dist-tag="$DIST_TAG"
