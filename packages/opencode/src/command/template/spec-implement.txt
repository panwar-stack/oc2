# Implement Specification Pull Request

Read and resolve $1 first. MUST use agent teams.

When PR number - $2 - is provided, implement only that pull request. Otherwise, complete $1 one pull request at a time. For each pull request, implement, test, review, verify, and commit before continuing.

Record the current branch, status, and committed HEAD as base_sha. 
Create one new branch, if there is no matching branch already with changes per given spec, from base_sha. Perform all edits, tests, reviews, and commits there.
If you created a new branch, then treat the current branch as read-only. Do not modify, merge into, fast-forward, rebase, cherry-pick, clean, reset, stash, switch, or commit in the original branch. 

Do not implement unrelated work. MUST Audit the full implementation when complete.